# Why?
- Previously took a course on computer architecture and assembly this got my gears going on some cool things, Initially I wanted to simulate cache memory but judging by my limited knowledge of the C language and inner workings of cache memory, 
I figured this small project was more fitting to experience the syntax and the memory aspect of C although given the size of arrays ill be working with allocating and de allocating memory shouldnt really be a concern.
- I always wanted something to fidget with and a small blackhjack game on my terminal would be very fun not only to play but to write, on top of everything i can add, currency, GUI , etc although if it comes to I might need to rewrite this in Java
  to make it easier to write.
- The shuffling algorithm is similar to the Fisher–Yates shuffle but less random so ill try to implement the Fisher - Yates shuffle later on when most of the game is complete
- IDK how a GUI might work but I can always try raylib
- Also once im done with the functions ill seperate them in different files to avoid nasty code formatting
